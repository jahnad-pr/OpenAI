# OpenAI Clone

A static OpenAI website clone — plain HTML, CSS, and JS, no build tools.

## Run Locally

Just open `index.html` in your browser. No install needed.

Or serve it locally:
```bash
git clone https://github.com/Jahanad-pr/OpenAI_.git
cd OpenAI_
npx serve .
```

